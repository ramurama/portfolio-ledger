PORTFOLIO_LEDGER_TRANSACTION_TYPES=Buy,Sell,Savings plan,Distribution,Taxes,Tax,Security transfer,Corporate action

# Optional: comma-separated portfolio:ISIN pairs (folder name, case-insensitive).
# PORTFOLIO_LEDGER_IGNORE_ISINS — used only with --apply-isin-ignore on process / generate-reports.
# PORTFOLIO_LEDGER_IGNORE_ISINS=rakshana:DE000EWG2LD7
# COSTBASIS_IGNORE_ISINS — always applied on generate-cost-basis (cost basis transfer report).
# COSTBASIS_IGNORE_ISINS=rakshana:DE000EWG2LD7,ramu:DE000EWG2LD7

# Optional: OpenFIGI API key for higher rate limits when fetching live quotes
# on the combined report (see https://www.openfigi.com/api).
# OPENFIGI_API_KEY=
